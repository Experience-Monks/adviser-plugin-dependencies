{
  "plugins": ["dependencies"],
  "rules": {
    "dependencies/min-vulnerabilities-allow": ["error", { "level": "low", "skip": ["780"] }],
    "dependencies/licenses-whitelist": ["warn", { "whitelist": ["MIT"] }]
  }
}
